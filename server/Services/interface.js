module.exports = (directory_name) => {
    // directory_name
    // const directory_name = require("./app");
    // var files = require('./Services/data-files-handling')(directory_name);
    // console.log(directory_name);
};